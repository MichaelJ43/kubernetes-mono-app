# Remote state (S3 + DynamoDB) — copy to `backend.hcl` (not committed) or pass `-backend-config=...`

bucket         = "YOUR_STATE_BUCKET"
key            = "kubernetes-mono-app/foundation/terraform.tfstate"
region         = "us-east-1"
encrypt        = true
dynamodb_table = "YOUR_LOCK_TABLE"
