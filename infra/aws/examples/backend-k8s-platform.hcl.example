# Same bucket/table/region as foundation; key uses same repo short name as GitHub.

bucket         = "YOUR_STATE_BUCKET"
key            = "kubernetes-mono-app/k8s-platform/terraform.tfstate"
region         = "us-east-1"
encrypt        = true
dynamodb_table = "YOUR_LOCK_TABLE"
