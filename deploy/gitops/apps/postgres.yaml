apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: postgres
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "1"
spec:
  project: default
  ignoreDifferences:
    # CloudNativePG continuously updates status; Git never matches → perpetual OutOfSync.
    - group: postgresql.cnpg.io
      kind: Cluster
      jsonPointers:
        - /status
        # Cluster created before Git added storageClassName: CNPG often cannot retrofit spec.storage.storageClassName on existing PVCs → live spec omits it.
        - /spec/storage/storageClassName
  source:
    repoURL: https://github.com/michaelj43/kubernetes-mono-app.git
    targetRevision: main
    path: deploy/base/postgres
  destination:
    server: https://kubernetes.default.svc
    namespace: portfolio
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
